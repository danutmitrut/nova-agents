{
  "name": "nova-slack-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Slack Socket Mode bridge for Nova Cortex / cortextOS agents.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@slack/bolt": "^4.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
